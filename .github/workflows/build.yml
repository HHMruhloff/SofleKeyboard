name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # Wir setzen BOARD_ROOT auf das aktuelle Arbeitsverzeichnis
      extra_cmake_args: "-DBOARD_ROOT=${{ github.workspace }}"
