name: Build ZMK Firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialize Workspace
        run: |
          west init -l .
          west update
          west zephyr-export

      - name: Build Left Side
        run: |
          # Dynamische Suche nach dem ZMK App Verzeichnis
          ZMK_APP_PATH=$(find ${{ github.workspace }} -type d -path "*/zmk/app" | head -n 1)
          if [ -z "$ZMK_APP_PATH" ]; then echo "ZMK App not found"; exit 1; fi
          
          west build -s "$ZMK_APP_PATH" -d build/left -b nice_nano_v2 -- \
          -DSHIELD=sofle_choc_pro_left \
          -DZMK_CONFIG="${{ github.workspace }}/config" \
          -DZMK_EXTRA_MODULES="${{ github.workspace }}"

      - name: Build Right Side
        run: |
          ZMK_APP_PATH=$(find ${{ github.workspace }} -type d -path "*/zmk/app" | head -n 1)
          
          west build -s "$ZMK_APP_PATH" -d build/right -b nice_nano_v2 -- \
          -DSHIELD=sofle_choc_pro_right \
          -DZMK_CONFIG="${{ github.workspace }}/config" \
          -DZMK_EXTRA_MODULES="${{ github.workspace }}"

      - name: Archive artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: |
            build/left/zephyr/zmk.uf2
            build/right/zephyr/zmk.uf2
