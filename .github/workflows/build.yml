name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # Wir "schmuggeln" das Modul über die Umgebungsvariable rein
      extra_cmake_args: "-DZEPHYR_EXTRA_MODULES=${{ github.workspace }}"
