name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
    with:
      # Wir lassen board/shield hier weg, da der Workflow sie hier nicht mag.
      # Stattdessen nutzen wir nur den config_path.
      config_path: "config"
