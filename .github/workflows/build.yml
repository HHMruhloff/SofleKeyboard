on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    # Der ZMK-Workflow nutzt keine 'with: board' Inputs.
    # Er sucht stattdessen nach build_matrix_path oder nutzt die Standard-Logik.
